const BASE_URL = process.env.BASE_URL
const API_ENDPOINTS = {
    GET_MESSAGES: BASE_URL + "/messages/",
    DELETE_MSG: BASE_URL + "/messages/",
}

const Constants = {
    API_ENDPOINTS,
}

export default Constants;